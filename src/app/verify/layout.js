export const metadata = {
  title: 'Verify Account | LeadLinx',
  robots: {
    index: false,
    follow: false,
  },
};

export default function VerifyLayout({ children }) {
  return <>{children}</>;
}
